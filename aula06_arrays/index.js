const alunos = ['luiz', 'maria', 'jose', 'pedro'];


alunos.push('luiza');
alunos.push('eduardo');

//console.log(alunos.slice(0, -1)); serve para fazer o fatiamento 
//(escolher os elementos que voce quer)

// delete alunos[1] eleta o elemento 

// alunos.unshift('diogo') unshift adiciona o elemento na primeira posição
// push adiciona na ultima
// pop remove o elemento do final da lista
// shift remove o elemento do inicio da lista